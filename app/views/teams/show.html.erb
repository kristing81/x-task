<div class="container">
  <div class="row">
    <div class="col-md-10">
      <h4><%= @team.name %></h4>
      <%= link_to "Edit Team", edit_team_path(@team), class: "btn btn-info" %>
      <%= link_to "Delete Team", @team, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this team?'} %>
      <!-- show teams tasks -->
    </div>
  </div>
</div>