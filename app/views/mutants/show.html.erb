<div class="container">
  <div class="row">
     <div class="col-md-10">
      <h4><%= @mutant.name %></h4>
      <%= link_to "Edit Mutant", edit_mutant_path(@mutant), class: "btn btn-info" %>
      <%= link_to "Delete Mutant", @mutant, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this mutant?'} %>
      <!-- show teams here -->
    </div>
  </div>
</div>
